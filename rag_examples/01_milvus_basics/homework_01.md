# 📝 Milvus 基础操作 — 课后作业

> **对应模块**: `01_milvus_basics`
> **难度**: ⭐⭐ | **建议用时**: 60-90 分钟
> **提交方式**: 新建 `homework/` 目录，将答案文件放入其中

---

## 一、理论题（共 5 题）

### 题目 1：Milvus 连接方式（简答题）

Milvus 提供了多种连接方式。请写出以下 3 种场景对应的 URI 格式：

| 场景 | URI 格式 |
|------|----------|
| (a) 本地 Milvus Lite 文件连接 | `_______________` |
| (b) 本地 Docker 服务连接 | `_______________` |
| (c) 带用户名密码认证的连接 | `_______________` |

> **提示**: 参考 `01_connect_milvus.py`

---

### 题目 2：度量类型选择（选择题）

以下 3 个场景，分别应该选择哪种度量类型（COSINE / L2 / IP）？

| 场景 | 你的选择 |
|------|----------|
| (a) 语义文本检索，使用 `text-embedding-v4` 模型（归一化向量） | `_______` |
| (b) 要求值越小表示越相似 | `_______` |
| (c) 向量未归一化，且需要考虑向量的绝对位置 | `_______` |

**请简要说明 (a) 选择该度量类型的原因：**

---

### 题目 3：索引类型匹配（连线题）

将左侧的数据规模场景与右侧推荐的索引类型连线：

```
(a) 数据量 5000 条              ① IVF_PQ
(b) 数据量 50 万条              ② FLAT
(c) 数据量 500 万条，内存充足    ③ IVF_FLAT
(d) 数据量 500 万条，要求最高精度 ④ HNSW
```

你的答案：(a) — ___  (b) — ___  (c) — ___  (d) — ___

---

### 题目 4：Collection 类比填空（填空题）

将 Milvus 概念与关系型数据库概念对应：

| Milvus 概念 | 关系型数据库概念 |
|------------|-----------------|
| Collection | `_______` |
| Field | `_______` |
| Primary Key | `_______` |
| Vector | （无直接对应，是一种特殊的 `_______` 列） |
| Index | `_______` |

---

### 题目 5：插入数据最佳实践（判断题）

判断以下说法的对错（✓ 或 ✗），错误的请简述原因：

| 说法 | 你的判断 | 原因（如果错误） |
|------|---------|----------------|
| (a) 单条插入性能优于批量插入 | `___` | |
| (b) 应该在插入数据前先创建索引 | `___` | |
| (c) `auto_id=True` 是推荐的做法 | `___` | |
| (d) 1024 维的向量可以插入 768 维的 Collection | `___` | |
| (e) 批量插入的推荐大小是 100-1000 条/批 | `___` | |

---

## 二、实操题（共 5 题）

> **注意**:
> - 所有代码写在 `homework/homework_practice.py` 文件中
> - 每个题目用 `print(f"\n== 实操题 N: 标题")` 分隔
> - 连接地址使用 `milvus_config.py` 的配置
> - 向量维度统一使用 **1024**

---

### 实操题 6：健康检查脚本

编写一个函数 `check_milvus_health()`，完成以下任务：

1. 连接 Milvus 服务
2. 检查服务器版本
3. 列出当前所有数据库
4. 列出当前所有 Collection
5. 如果连接失败，打印友好的错误提示

**验收标准**: 运行脚本后，能正常输出上述所有信息。

---

### 实操题 7：创建「学生成绩」Collection

创建一个名为 `student_scores` 的 Collection，包含以下字段：

| 字段名 | 数据类型 | 说明 |
|--------|---------|------|
| `id` | INT64 | 主键，自增 |
| `student_name` | VARCHAR(64) | 学生姓名 |
| `subject` | VARCHAR(32) | 科目 |
| `score` | INT64 | 分数 |
| `embedding` | FLOAT_VECTOR | 科目描述的向量（1024 维） |

创建完成后，使用 `describe_collection()` 打印所有字段信息。

**验收标准**: Collection 创建成功，字段信息打印正确。

---

### 实操题 8：批量插入学生数据

向 `student_scores` Collection 批量插入以下 8 条数据：

```python
students = [
    {"student_name": "张三", "subject": "数学",     "score": 95, "desc": "擅长代数与几何"},
    {"student_name": "李四", "subject": "语文",     "score": 88, "desc": "写作能力突出"},
    {"student_name": "王五", "subject": "英语",     "score": 92, "desc": "口语流利，阅读广泛"},
    {"student_name": "赵六", "subject": "物理",     "score": 78, "desc": "力学基础扎实"},
    {"student_name": "孙七", "subject": "化学",     "score": 85, "desc": "实验操作能力强"},
    {"student_name": "周八", "subject": "历史",     "score": 90, "desc": "中国古代史专家"},
    {"student_name": "吴九", "subject": "地理",     "score": 82, "desc": "世界地理知识丰富"},
    {"student_name": "郑十", "subject": "生物",     "score": 91, "desc": "分子生物学研究方向"},
]
```

要求：
1. 使用 **模拟 Embedding** 生成 1024 维向量（`desc` 字段作为输入）
2. 使用 **批量插入**（一次插入所有 8 条）
3. 插入完成后打印插入结果和总行数

**验收标准**: 8 条数据全部插入，`row_count` 显示 8。

---

### 实操题 9：创建索引 + 向量检索

1. 为 `student_scores` Collection 的 `embedding` 字段创建 **IVF_FLAT** 索引
   - `nlist` 设置为 4
2. 加载 Collection
3. 用一条测试查询向量（模拟 `"数学相关"` 的向量）进行检索
4. 返回 Top 3 最相似的结果，打印相似度分数和内容

**验收标准**: 能成功检索并返回 3 条结果，每条包含 `distance` 分数和学生信息。

---

### 实操题 10：数据库管理 + 完整流程

编写一个函数 `demo_full_workflow()`，演示从连接 → 建库 → 建表 → 插入 → 检索 → 清理 的完整流程：

1. 连接 Milvus
2. 创建一个名为 `wolin_homework_db` 的数据库（如果已存在则先删除）
3. 连接到该数据库
4. 在该数据库中创建一个简单的 Collection（dimension=1024）
5. 插入 5 条模拟数据
6. 创建 FLAT 索引并检索 Top 2
7. **清理**: 删除 Collection 和 `wolin_homework_db` 数据库

**验收标准**: 完整流程无报错，最后数据库被正确清理（`list_databases()` 中不再包含 `wolin_homework_db`）。

---

## 📊 评分标准

| 题目 | 类型 | 分值 | 评分要点 |
|------|------|------|---------|
| 题目 1 | 理论 | 10 分 | 3 种 URI 格式正确 |
| 题目 2 | 理论 | 10 分 | 选择正确 + 原因合理 |
| 题目 3 | 理论 | 10 分 | 4 组匹配全部正确 |
| 题目 4 | 理论 | 10 分 | 5 个填空全部正确 |
| 题目 5 | 理论 | 15 分 | 5 个判断 + 错误原因 |
| 实操题 6 | 实操 | 10 分 | 健康检查功能完整 |
| 实操题 7 | 实操 | 10 分 | Collection 字段定义正确 |
| 实操题 8 | 实操 | 10 分 | 批量插入 + 模拟 Embedding |
| 实操题 9 | 实操 | 10 分 | 索引创建 + 检索结果正确 |
| 实操题 10 | 实操 | 15 分 | 完整流程 + 正确清理 |
| **总计** | | **120 分** | |

---

## ✅ 自检清单

完成作业后，请确认：

- [ ] 理论题答案填写在 `homework/homework_answers.md` 文件中
- [ ] 实操题代码写在 `homework/homework_practice.py` 文件中
- [ ] 实操题代码可以完整运行（`python homework/homework_practice.py`）
- [ ] 向量维度统一为 **1024**
- [ ] 没有硬编码 IP 地址或数据库名
- [ ] 每个实操题都有清晰的打印输出
